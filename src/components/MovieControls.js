import React from 'react'

export const MovieControls = ({movie}) => {
    return (
        <div className="inner-card-controls">
            {type === 'favoriteslist' && (
                
            )}
        </div>
    )
}
